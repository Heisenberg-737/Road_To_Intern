#include <bits/stdc++.h>

using namespace std;

void search_bin(vector<int> &arr)
{
    
}

int main(int args, char **argv)
{
    int n, data;
    cin >> n >> data;
    vector<int> arr(n, 0);

    for (int i = 0; i < n; i++)
        cin >> arr[i];

    search_bin(arr);
}